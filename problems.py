from task import Task

root_task = Task('Solve this whole problem', 'Place opening and closing brackets into the input string like this: for odd length: example → e(x(a(m)p)l)e; for even length: card → c(ar)d, but not c(a()r)d. The program receives a string of English letters (lowercase and uppercase) as an input. Print out the string with the brackets added.')
